{{- if .Values.oidc.enabled -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "library-chart.fullname" . }}-oidc
  labels:
    {{- include "library-chart.labels" . | nindent 4 }}
type: Opaque
stringData:
  OAUTH2_PROXY_COOKIE_SECRET: {{ .Values.oidc.cookieSecret | quote }}
{{- end }}
